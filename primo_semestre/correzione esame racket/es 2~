;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-intermediate-lambda-reader.ss" "lang")((modname |es 2|) (read-case-sensitive #t) (teachpacks ((lib "hanoi.ss.txt" "installed-teachpacks"))) (htdp-settings #(#t constructor repeating-decimal #f #t none #f ((lib "hanoi.ss.txt" "installed-teachpacks")) #f)))
(define lcs
  (lamba

(define rec-proc
  (lambda (i u j v)
    (cond ((or (string=? u "")(string=? v ""))
           (list null null)
           )
          ((char=? (string-ref u 0)(string-ref v 0))
           (let ((x (rec-proc
                     (+ i 1)
                     (substring u 1)
                     (+ j 1)
                     (substring v 1)))
                 )
             (list XXXX XXXX)
             ))
          (else
           (let ((x (rec-proc (+ i 1)(substring u 1) j v))
                 (y (rec-proc XXXX u XXXXX))
                 )
             (if (< XXXXX XXXXX)
                 x
                 y)
             ))
          )))
    